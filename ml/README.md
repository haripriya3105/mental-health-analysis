# ML Workspace

This directory is reserved for future machine-learning work. No models, datasets, or AI/ML implementation are included in the initial setup.

Planned organization:

- `data/` — local data files (kept out of Git when appropriate)
- `notebooks/` — exploration notebooks
- `src/` — future ML source code
- `models/` — trained model artifacts (kept out of Git when appropriate)
